---
import { Image } from "astro:assets";
import VideoCall from "../../assets/Protection/VideoCall.png";
---

<section class="xl:container">
  <!-- ================= HEADING (unchanged) ================= -->
  <div class="flex flex-col justify-center text-center mx-[120px]">
    <h2 class="text-5xl font-bold">
      How we actually make this happen in your bathroom
    </h2>
    <p class="mt-5">
      We don’t just drop off products. We visit, plan, install and test
      everything with you.
    </p>
  </div>

  <!-- ================= STEPS + STICKY IMAGE ================= -->
  <div class="mt-[100px] flex flex-row gap-[60px] justify-center mx-[120px]">
    <!-- LEFT: scrolling steps -->
    <div class="space-y-[259px]">
      <div class="flex gap-3 max-w-[405px] px-6">
        <div class="w-[2px] bg-[#CC0000]"></div>
        <div>
          <h4 class="text-2xl font-bold">Quick call & photos</h4>
          <p class="mt-3 text-sm text-gray-600">
            You tell us the bathroom, we ask a few photos or a video.
          </p>
        </div>
      </div>

      <div class="flex gap-3 max-w-[405px] px-6">
        <div class="w-[2px] bg-[#CC0000]"></div>
        <div>
          <h4 class="text-2xl font-bold">On-site safety walk-through</h4>
          <p class="mt-3 text-sm text-gray-600">
            We visit the site, understand risky zones and mark them.
          </p>
        </div>
      </div>

      <div class="flex gap-3 max-w-[405px] px-6">
        <div class="w-[2px] bg-[#CC0000]"></div>
        <div>
          <h4 class="text-2xl font-bold">Layout sketch & installation</h4>
          <p class="mt-3 text-sm text-gray-600">
            Custom layout, careful installation without damaging tiles.
          </p>
        </div>
      </div>

      <div class="flex gap-3 max-w-[405px] px-6">
        <div class="w-[2px] bg-[#CC0000]"></div>
        <div>
          <h4 class="text-2xl font-bold">Test run & care tips</h4>
          <p class="mt-3 text-sm text-gray-600">
            We test everything and show how to clean and maintain it.
          </p>
        </div>
      </div>
    </div>

    <!-- RIGHT: sticky image -->
    <div class="hidden lg:block">
      <div class="sticky top-[120px]">
        <Image
          src={VideoCall}
          alt="Installation consultation"
          class="max-w-[687px] rounded-lg"
        />
      </div>
    </div>
  </div>
</section>
