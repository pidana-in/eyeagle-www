---
import styles from "../../styles/HomeV3/button-link.module.css";

interface Props {
  href: string;
  size?: "large" | "small";
  variant?: "solid" | "inverse" | "glass";
  class?: string;
  tabindex?: number;
  [key: string]: unknown;
}

const {
  href,
  size = "large",
  variant = "solid",
  class: className,
  tabindex,
  ...rest
} = Astro.props;
---

<a {...rest} class:list={[styles.button, styles[size], className]} data-variant={variant} href={href} tabindex={tabindex}>
  <slot />
</a>
