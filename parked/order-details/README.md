# Parked order-details feature

This prototype is intentionally outside `src/pages`, so Astro does not publish
an order-details route. Restore it only when Shopify supplies the authenticated
order data needed to replace the placeholder customer and delivery details.
